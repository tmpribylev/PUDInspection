@model List<PUDInspection.ViewModels.UsersInspectionSpaceViewModel>

@{
    var spaceId = ViewBag.spaceId;
    ViewBag.Title = "Добавить или удалить модераторов";
}

<form method="post">
    <div class="card">
        <div class="card-header">
            <h2>Добавить или удалить модераторов данного пространства</h2>
        </div>
        <div class="card-body">
            @for (int i = 0; i < Model.Count; i++)
            {
                <div class="form-check m-1">
                    <input type="hidden" asp-for="@Model[i].UserId" />
                    <input type="hidden" asp-for="@Model[i].UserName" />
                    <input asp-for="@Model[i].IsSelected" class="form-check-input" />
                    <label class="form-check-label" asp-for="@Model[i].IsSelected">
                        @Model[i].UserName
                    </label>
                </div>
            }
        </div>
        <div class="card-footer">
            <input type="submit" value="Обновить" class="btn btn-primary"
                   style="width:auto" />
            <a asp-action="EditInspectionSpace" asp-route-id="@spaceId"
               class="btn btn-primary" style="width:auto">Отмена</a>
        </div>
    </div>
</form>